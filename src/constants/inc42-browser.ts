export const INC42_READ_MORE_TEXT = 'Read More Stories';
export const INC42_READ_MORE_MAX_CLICKS = 6;
export const INC42_READ_MORE_SETTLE_MS = 1500;

export const INC42_LATEST_NEWS_TEXT = 'Latest News';
/** Small nudge scroll for feed navigation (px). */
export const INC42_SCROLL_STEP_PX = 400;
/** Max micro-scrolls per Read More hunt or recovery round. */
export const INC42_SCROLL_MAX_STEPS = 16;
export const INC42_SCROLL_SETTLE_MS = 500;
/** Max micro-scrolls to reach the Latest News section from page top. */
export const INC42_ANCHOR_MAX_STEPS = 12;
/** After Read More click, small scrolls to recover from page jump-up. */
export const INC42_POST_CLICK_RECOVERY_STEPS = 10;
/** Stop feed scroll when this many consecutive steps add no new article links. */
export const INC42_FEED_IDLE_STEPS = 3;

/** Saved auth state for agent-browser (exported by npm run inc42:login). */
export const INC42_AUTH_STATE_FILE = 'inc42-auth.json';

export const INC42_DEFAULT_DEBUG_PORT = 9222;
